'use client';

// This component has been removed since we no longer need theme switching.
// We're returning null to prevent rendering anything.
const ThemeSwitcher = () => {
  return null;
};

export default ThemeSwitcher;